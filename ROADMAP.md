# roadmap

what i haven't gotten around to yet

+ turn server for voice
+ outgoing emails
+ MAS server to support element X mobile app (element classic works fine)
+ integrate `keycloak-magic-link` for easy user rego
